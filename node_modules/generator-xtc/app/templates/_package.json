{
  "name": "<%= name %>",
  "version": "0.0.0",
  "private": true,
  "repository": "<%= repository.uri %>",
  "dependencies": {
    "xtc": "<%= xtcVersion %>",
    "generator-xtc": "*"
  },
  "bundledDependencies": [
    "generator-xtc"
  ],<% if (needServer) { %>
  "main": "server.js",<% } %>
  "configPath": "_config/"
}

{
  "name": "<%= name %>",
  "version": "0.0.0",
  "private": true,
  "repository": "<%= repository.uri %>",
  "dependencies": {
    "xtc": "<%= xtcVersion %>",
    "generator-xtc": "*"<% if (needServer) { %>,
    "hipsum": "~0.2.0"<% } %>
  },
  "bundledDependencies": [
    "generator-xtc"
  ],<% if (needServer) { %>
  "main": "server.js",
  "engines": { "node" : "~0.10.30" },<% } %>
  "configPath": "config/"
}

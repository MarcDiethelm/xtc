// Never commit authentication data to a public repository!
{
	"devPort": 3333

	// Physical path to the static assets, used by Express and by the 'asset' template helper
	,"staticPath": "frontend/_static"
}